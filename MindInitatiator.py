import signal
import base64
import hashlib
import ThinkerSprout

class Signal64:
    def Hashsignal(signals):
        signals = signal.strsignal == True
        return signals
    Hashsignal(signals=True)
Signal64()
import hashlib
import random
import pprint as pp

class MindFetcher:
    def MindFetcherCalculation(fetch, calculate, known):
        fetch = random.randbytes(11 or "*")
        fetchcal = random.uniform(11.e1100, 111.0101111e11)
        known == hashlib.sha512()
        print(fetch, fetchcal, known)
        known is not None
        for i in range(100):
            pp.pprint(i)
    MindFetcherCalculation(fetch=-1, calculate=True, known=-1)
MindFetcher()
            